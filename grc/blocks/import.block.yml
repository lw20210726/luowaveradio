id: import_
label: python函数库导入器
flags: [ python ]

parameters:
-   id: imports
    label: Import
    dtype: import

templates:
    imports: ${imports}

documentation: |-
    Import additional python modules into the namespace.

    Examples:
    from luowaveradio.filter import firdes
    import math,cmath
    from math import pi

file_format: 1
